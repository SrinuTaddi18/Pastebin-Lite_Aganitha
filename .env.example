# MongoDB connection string (your Atlas cluster)
# Replace <db_password> with your actual database user password.
MONGODB_URI=mongodb+srv://taddisrinu2004:<db_password>@tutorials.doi7w.mongodb.net/pastebin?retryWrites=true&w=majority&appName=Tutorials

# In development: frontend URL so "Create a new paste" and GET / work (use the port Vite shows, e.g. 3001)
# FRONTEND_URL=http://localhost:3001

# Optional: override base URL for paste links (production)
# APP_URL=https://your-app.vercel.app
